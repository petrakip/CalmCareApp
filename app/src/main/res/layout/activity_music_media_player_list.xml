<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MusicMediaPlayerList">

    <Button
        android:id="@+id/goBackBtn"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_margin="10dp"
        android:background="@drawable/arrow_back_icon"
        android:contentDescription="@string/go_home"
        app:layout_constraintBottom_toBottomOf="@+id/musicPlayerTitle"
        app:layout_constraintEnd_toEndOf="@+id/musicPlayerTitle"
        app:layout_constraintHorizontal_bias="0.043"
        app:layout_constraintStart_toStartOf="@+id/musicPlayerTitle"
        app:layout_constraintTop_toTopOf="@+id/musicPlayerTitle"
        app:layout_constraintVertical_bias="1.0" />

    <TextView
        android:id="@+id/musicPlayerTitle"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:background="#4DB6AC"
        android:gravity="center"
        android:text="@string/music_player_tracklist"
        android:textAlignment="center"
        android:textColor="@color/black"
        android:textSize="20sp"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ListView
        android:id="@+id/songListView"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:divider="@android:color/darker_gray"
        android:dividerHeight="1dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/musicPlayerTitle"
        app:layout_constraintVertical_bias="0.0"
        android:background="@drawable/rectangle1"/>

</androidx.constraintlayout.widget.ConstraintLayout>